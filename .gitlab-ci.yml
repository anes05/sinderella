# .gitlab-ci.yml
image: alpine:latest

pages:
  publish: public
  rules:
    - if: $CI_COMMIT_REF_NAME == "dev"   # 👈 changed from $CI_DEFAULT_BRANCH
  script:
    - echo "Deploying static site from dev branch"